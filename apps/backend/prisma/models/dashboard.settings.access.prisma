// Main access model for dashboard permissions
model DashboardSettingsAccess {
  id      Int      @id @default(autoincrement())
  users   DashboardSettingsAccessUser[]
  groups  DashboardSettingsAccessGroup[]
  settings DashboardSettings?
}
